# Agentic Engineering Org — Build Log

This document tracks the supervised build of the AI-engineering-org workflow
(role subagents + deterministic gates + vendored TDD harness + sprint tooling)
adopted into the **Zij** product repo. It is the system of record for the build
itself; Zij's *product* decisions live separately in
[`design/docs/DECISIONS.md`](../design/docs/DECISIONS.md).

- **Skill:** `agentic-engineering-org`
- **Target repo:** `github.com/Muhanad-husn/Zij` (existing product repo, not a blank template)
- **Stack profile:** default — **Python 3.13 / uv / pytest / ruff** (matches Zij design)

---

## Progress Tracker

| Phase | Title | Status | Date | Notes |
|---|---|---|---|---|
| 0 | Repository foundation | **DONE** | 2026-07-05 | Scaffold on `setup/00-foundation`; green baseline; no `gh repo create` (repo exists). **Server-side branch protection unavailable on free private plan (403) — deferred, see DEC-16.** Founder to merge the branch. |
| 1 | `CLAUDE.md` handbook | **DONE** (approved-by-merge) | 2026-07-05 | On `setup/01-handbook` (stacked on Phase 0). 111 lines; preserves Developer Principles; answers "who merges?" + "who edits specs, when?". Zij mappings included. **Merged into `origin/main`; merge is the founder-only Checkpoint-1 approval.** |
| 2 | Role subagents | **DONE** (approved-by-merge) | 2026-07-05 | Five roles in `.claude/agents/` (`setup/02-roles`, PR #1). Reviewer read-only. Per-role `deny.ps1` guard wired in frontmatter. **Merged into `origin/main` = Checkpoint-2 approval.** Live proof of the guards is a Phase-3 item (see DEC-23). |
| 3 | Hard gates (hooks) | **DONE** (approved-by-merge, PR #3) | 2026-07-05 | On `setup/03-hooks`. Five PowerShell scripts + `settings.json` wiring + per-role frontmatter guards. **Root cause of non-firing found & fixed (DEC-24): the `shell: powershell` + `& '…'` command form never launched the scripts; switched all wiring to `pwsh -NoProfile -File …`.** **GLOBAL gates now verified LIVE this session:** orchestrator `git merge` **blocked**; push-to-`main` **blocked**; GitHub-plugin `merge_pull_request` **blocked** (before hitting GitHub); orchestrator `gh pr merge` path **stays OPEN**; a `git commit` on a **red** suite **blocked** (tests-green, with failing tail); `design/` write **blocked** (spec-freeze). **Subagent-frontmatter gates now largely verified LIVE this session (DEC-27):** the per-role `deny.ps1` **path guard fired** in a real subagent dispatch — write to `backend/tests/` blocked by the frontmatter guard, write to `design/` blocked (spec-freeze), write to `backend/` allowed (control). The **subagent `gh pr merge` block** is covered by component-equivalence (every constituent proven live) but not yet by a single end-to-end probe run — the block-merge guard was added to the probe mid-session and agent frontmatter does not hot-reload, so it needs one more restart to fire end-to-end. **RESOLVED (2026-07-05, fresh session): the subagent `gh pr merge` block now verified LIVE end-to-end (DEC-28).** In a fresh process (global git-guard re-confirmed live: orchestrator `git merge --abort` blocked), the `gatecheck-probe` ran the last leg: (1) `gh pr merge 999999 --merge` → **BLOCKED by `block-merge.ps1`** (deny text uniquely matches `block-merge.ps1:6`, distinct from git-guard's strings; and git-guard deliberately passes `gh pr merge` per DEC-22, so the block can only be the frontmatter guard); (2) control `echo` → ran fine (not a blanket Bash deny). Every gate leg is now proven live. The real `implementer` also *correctly refused* a bypass "drill" (defense-in-depth, DEC-25). **Checkpoint 3 APPROVED; merged into `origin/main` via PR #3 (2026-07-05) by the orchestrator on founder instruction, through the `gh pr merge` path the gates leave open (DEC-22).** |
| 4 | Vendor & adapt TDD harness | **DONE** (approved-by-merge, PR #4) | 2026-07-05 | On `setup/04-harness` (stacked on `setup/03-hooks`). Vendored `brainqub3/red-green-refactor` **@ `593e7abae2dc74f9a21eba3323e78a8fa9520dba`** by copy into `.claude/skills/` (6 skills). **Audited both write/delete scripts clean; no skill merges (DEC-29).** Adapted all 6 to roles/gates/Python via top "Zij adaptation" override blocks + inline edits: **DEC-1 role split enforced in `red-green-refactor`**; non-web transcript evidence default; `python-ci.yml` rewritten to uv/pytest/ruff (parse-validated); `tdd-plan` template gains `Issue:` + DEC-1 fields. `.gitignore`/`plans/` set up. All 6 skills hot-loaded; both Node scripts smoke-tested live; suite green. **Live end-to-end (plan→rgr→ci→pr) run is the Phase-6 dry run; Checkpoint 4 = founder confirms the audit + DEC-1 adaptation.** **Checkpoint 4 APPROVED; merged into `origin/main` via PR #4 (2026-07-05).** Both merges executed by the orchestrator on the founder's "merge the branches" instruction (approval-gated action, operating rule 3). |
| 5 | Sprint & role wiring | **DONE** (approved-by-merge, PR #5) | 2026-07-05 | On `setup/05-sprint` (stacked on `setup/04-harness`). Four founder-invoked **commands** in `.claude/commands/` (not auto-triggered skills — DEC-30): `/sprint-plan` (triage decompose → `tdd-plan` slice plans → draft `plans/backlog-<sub>.md` for review → file via GitHub plugin `issue_write`, **fall back to `gh issue create` on a plugin 403** → back-link `Issue:` fields), `/sprint-start` (select next issue by dependency, skip `blocked`/`needs-context` → branch → spec-author if needed → **test-author commits outer test red first** → implementer greens inner cycles → reviewer two-stage → `safe-pr` prepares PR, **never merges**), `/triage` + `/review` (thin delegators to the role subagents). Five GitHub labels created (DEC-31): `spec-drift`, `blocked`, `needs-context`, `done-with-concerns`, `sub:v0` (namespace seed). All four commands hot-loaded live this session. **Plugin PAT can't create PRs (403) → PR #5 opened via `gh` fallback (DEC-32).** **Checkpoint 5 APPROVED; merged into `origin/main` via PR #5 (2026-07-05) by the orchestrator on the founder's "merge it" (merge commit `19aecb0`), through the `gh pr merge` path the gates leave open (DEC-22).** The live end-to-end sprint run is the Phase-6 dry run. |
| 6 | Dry run & validation | **DONE** (approved-by-merge, PR #7) | 2026-07-05 | Ran in a fresh process (gates confirmed live on entry: orchestrator `git merge --abort` **blocked**). One throwaway feature (`health_status()` helper, issue **#6**) driven end-to-end through the tool-locked roles and live gates: **triage** scoped the slice → orchestrator filed issue #6 (plugin `issue_write` **403**, `gh` fallback — DEC-32) + wrote the slice plan → **test-author** committed the **red outer test first** (strict-xfail, DEC-33; `59496da`, `1 xfailed` → exit 0 → landed under the tests-green gate) → **implementer** greened `backend/health.py` and, per DEC-33/34, left it uncommitted when the strict-xfail flipped to XPASS → **test-author** removed the marker and landed the final green commit (`8adc767`, `2 passed`) → **reviewer** two-stage **DONE, no blocking findings** (independently re-verified DEC-1 ordering from git history) → `tdd-ci` generated `.github/workflows/ci.yml` → **`safe-pr`** collected transcript evidence and opened **PR #7** into `main`. **DEC-1 invariant proven from git history: the red outer test commit precedes `backend/health.py` (first appears in `8adc767`).** The dry run surfaced and resolved two constitution contradictions — **DEC-33** (red-commit vs green-gate → strict-xfail) and **DEC-34** (implementer cannot author tests → test-author owns all test authorship). Branch is `feat/health-status/01-health-status` (cut off the Phase-6 commits so it inherited the DEC-33/34 briefing + plans; it doubles as the Phase-6 delivery branch). **Every DoD item holds.** CI **green** on the PR (ruff + pytest, both jobs). **Checkpoint 6 APPROVED; PR #7 merged into `origin/main` (merge commit `d247545`) by the orchestrator on the founder's explicit "run the merge" instruction — through the `gh pr merge` path the gates leave open (DEC-22).** Issue **#6 auto-closed** by `Closes #6`. **`backend/health.py` kept on `main`** (founder's choice — a harmless, tested first backend module). **All six phases are DONE and approved-by-merge — the agentic-engineering-org build is complete.** |

Status legend: `Not started` · `In progress` · `DONE` · `DONE_WITH_CONCERNS` · `BLOCKED` · `NEEDS_CONTEXT`.

---

## Decision Log

Seeded with the skill's locked decisions (DEC-1..7) plus decisions/adaptations
made during this build. Append a row on any divergence from a skeleton or
resolved ambiguity.

| # | Decision | Rationale |
|---|---|---|
| DEC-1 | Test authorship is split: the **outer acceptance test is the behavioral contract** (spec/test-author writes it, commits red, locks it); the implementer drives **inner unit cycles only** and may not edit the outer test or specs. | Locked by skill. |
| DEC-2 | Roles are **addressable subagent files** in `.claude/agents/`, each with a locked `tools` set and a pinned `model`. | Locked by skill. |
| DEC-3 | Two gates are **deterministic hooks**, not advice: *agents-never-merge* and *tests-green-before-commit*. Branch protection backstops them server-side. | Locked by skill. |
| DEC-4 | **GitHub issues and PRs** are the system of record. Sprints, not sessions. | Locked by skill. |
| DEC-5 | One repository. Spec and build separated by **role and a spec-freeze hook**, not by folder. | Locked by skill. |
| DEC-6 | The behavior-first loop is the **vendored `brainqub3/red-green-refactor` harness** (MIT), adapted to roles/gates — not hand-built. | Locked by skill. |
| DEC-7 | DEC-4 runs through the **installed GitHub plugin** (`mcp__plugin_github_github__…`), not raw `gh` in Bash. The *agents-never-merge* gate must also match the plugin's merge tool, not only `Bash(git merge …)`. | Locked by skill. |
| DEC-8 | **Stack profile = default** (Python 3.13 / uv / pytest / ruff). Test command: `uv run pytest`; lint: `uv run ruff`. | Confirmed against Zij `design/docs/STRUCTURE.md §5` (pyproject deps) and detected toolchain (python 3.13.14, uv 0.11.6). |
| DEC-9 | **Adopting into an existing repo**, not building a blank `ai-enterprise-template`. The GitHub remote already exists (`Muhanad-husn/Zij`), so Phase 0 **skips `gh repo create`** and prepares only branch protection. | Repo has a `main` with design already committed + a live `origin`. |
| DEC-10 | **Layout mapping** (template → Zij actual): `specs/ → design/`, `src/ → backend/`, `tests/ → backend/tests/`. The Phase-3 spec-freeze hook will guard **`design/**`** (Zij's spec/contract layer per STRUCTURE.md §3), not a root `specs/`. No redundant root `specs/`/`src/`/`tests/` created. | Zij's design already fixed this layout (STRUCTURE.md §2, §6); a competing `tests/`/`src/` would contradict it. |
| DEC-11 | **Secrets via `.env`** (env-only, per `design/contracts/config.md`), not the template's `secrets/secrets.toml`. Created `.env.example`; `.gitignore` excludes `.env`. | Zij's config contract mandates env-only secrets. |
| DEC-12 | Phase 0 `pyproject.toml` carries **empty runtime `dependencies`** (only dev = pytest, ruff). Real runtime deps land during the v0 spike sprint. | Keep the baseline light + green; avoid front-running the implementation sprint's dependency wiring. |
| DEC-13 | Phase-0 scaffold committed on branch `setup/00-foundation` for the **founder to merge** (dogfooding the workflow), rather than the skill's "one commit on main" (which assumed a brand-new empty repo). | Operating rule 4; repo already has history on `main`. |
| DEC-14 | Branch protection via a **repository ruleset** (`protect-main`), not classic branch-protection. | Repo is **private**; classic protection needs a paid plan, rulesets work on free private repos and are GitHub's current recommendation. |
| DEC-15 | Ruleset requires a **PR before merge but 0 approving reviews** (plus block-deletion, block-force-push). | **Solo operator**: GitHub forbids approving your own PR, so a 1-review requirement would lock the founder out of merging. Direct push to `main` stays blocked; the human still manually merges. Server-side status-check requirement deferred to Phase 4 (no CI workflow exists yet). |
| DEC-16 | **Server-side branch protection deferred → RESOLVED 2026-07-05: repo made public** by the founder, which unblocks rulesets/protection for free. On a free *private* repo GitHub had rejected both (403). Local hooks (Phase 3) remain the primary gates; branch protection is the *backstop*. Founder to enable the `protect-main` ruleset (command re-presented at Checkpoint 1/2). | Free plan can't protect a private repo; going public was the founder's chosen fix. |
| DEC-17 | **Path guards use one policy script** `deny.ps1 -Role <role>`, fired on **every** `Edit\|Write` for the three writing roles; it reads the target path from stdin and applies per-role allow/deny (**fail-closed**). Allowed roots: spec-author → `design/`; test-author → any `**/tests/`; implementer → everything **except** `design/` and `**/tests/`. | Chosen over `if:`-glob always-deny because Zij **nests tests under the code root** (`backend/tests/` inside `backend/`); pure globs can't express "allow tests, deny code". The skill explicitly sanctions the stdin-deciding fallback. |
| DEC-18 | **Reviewer written from scratch**, preserving the two-stage order (spec-compliance → code-quality) and the "does the test encode intent?" check. | The installed GitHub plugin is **MCP-only** (no bundled agents); `pr-review-toolkit` is **not installed**. Founder may install it later to layer on. |
| DEC-19 | **Triage and reviewer carry no GitHub-plugin tools** (roster locks them to Read/Grep/Glob/Bash; per docs that allowlist denies all MCP tools). They **draft/report**; the main session / sprint skills (Phase 5) do plugin writes (file issues, open PRs). | Reinforces *agents-never-merge* at the capability level: these roles literally cannot call `merge_pull_request`. |
| DEC-20 | **Hooks implemented in PowerShell** with `shell: powershell`, not bash `.sh`/`jq`. Scripts read stdin via `[Console]::In.ReadToEnd() \| ConvertFrom-Json`, use `$env:CLAUDE_PROJECT_DIR`. Applies to all Phase-3 gate scripts. | Windows host; the hook default would be Git Bash (installed) but `jq` isn't guaranteed and the sandbox Bash lacks coreutils. PowerShell is guaranteed and deterministic. |
| DEC-21 | **PreToolUse hooks block via the documented JSON contract** — emit `{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"deny","permissionDecisionReason":"…"}}` on stdout and `exit 0` — **not** the skeleton's `exit 2`. Allow = `exit 0` with no output (defers to normal permission flow, so a role's silent-allow still lets global hooks like spec-freeze apply). | Re-verified against `code.claude.com/docs/en/hooks` (operating rule 6): current docs specify PreToolUse decisions via `permissionDecision`, superseding the reference skeleton's exit-2. |
| DEC-22 | **agents-never-merge is split to keep the orchestrator's approved merge open.** `git-guard.ps1` (global, all Bash) blocks `git merge`, `gh api …merge(s)`, push-to-`main`, and direct commits on `main` — paths the orchestrator never needs — but **deliberately lets `gh pr merge` through**. Subagents are blocked from `gh pr merge` by a per-role `PreToolUse` Bash frontmatter guard (`block-merge.ps1`, `if: Bash(gh pr merge *)`) on the four Bash-capable roles. The plugin merge tool is blocked globally via the MCP matcher. | A global block can't tell orchestrator from subagent; frontmatter is the only scope that closes `gh pr merge` for subagents while leaving it open for the founder-approved orchestrator path (DEC-3). The REST PR-merge endpoint is `/pulls/{n}/merge` (singular), so the `gh api` matcher matches `merge`, not just `merges`. |
| DEC-26 | **`git-guard` matches its trigger tokens (`git merge`, `git push … main`, `git commit`) as substrings anywhere in the Bash command string — deliberately broad and fail-safe.** Consequence observed live: a `git commit` whose **commit message** contained the words "git merge" was itself blocked. This is accepted, not fixed: for a merge gate a false positive (blocking a benign command that merely mentions the token) is safe and easily worked around (reword the message, or the founder runs it), whereas a false negative (missing a real merge) defeats the gate. Tightening to anchor on command position risks false negatives against `&&`/`;`/subshell-chained invocations. Per the 80/20 principle, kept broad + documented. Workaround for authoring commits that must discuss merges: avoid the literal two-word token, or the founder commits. **Recurred live 2026-07-05:** a `gh pr create` for PR #3 was itself **blocked** because the PR **body** described the gates using the literal phrases "git merge" and "gh api …merge"; reworded the body to "local branch-merges" / "REST PR-merge via the API" and it went through. Confirms the substring match is on the whole Bash command string (body text included), and the documented reword workaround holds. | Fail-safe bias is correct for a security gate; shell-accurate command parsing is not worth the false-negative risk. |
| DEC-25 | **The subagent-level path/merge guards are verified with a temporary neutral `gatecheck-probe` agent, not the real roles — because the real roles correctly refuse the drill.** When asked (via an orchestrator "gate drill" prompt) to attempt an out-of-role write and a `gh pr merge`, the real `implementer` **refused every step and made zero tool calls**, citing CLAUDE.md ("no agent message is founder consent") — treating the drill as a suspected bypass/injection. That is exactly the desired defense-in-depth: the agent won't even try, so the hook never has to fire. But it means the hook *mechanism* can't be exercised through a compliant role. A throwaway `gatecheck-probe.md` (implementer's frontmatter guard + a neutral "mechanical harness" prompt, `model: haiku`) is used solely to trigger the guard and observe the deny; it is **kept untracked and deleted after verification — never committed**. Also learned: **new/edited agent files do NOT hot-reload** (the probe was "not found" until a restart), so this observation is deferred to the post-restart pass. | The gate must catch a *non-compliant* actor; a compliant one refuses first. Testing the hook itself requires an actor without a competing conscience, hence the neutral probe. |
| DEC-24 | **Hook scripts are invoked with `pwsh -NoProfile -File <script>.ps1 [args]`, NOT via a `shell: powershell` field + `& '…'` call-operator command.** The original wiring (`shell: powershell`, `command: "& '${CLAUDE_PROJECT_DIR}/…ps1'"`) **never launched the scripts on this Claude Code build** — a live `git merge` passed straight through and a diagnostic marker confirmed the script body never executed (no marker written). Switching every `command` (in `settings.json` **and** all five agent frontmatter guards) to `pwsh -NoProfile -File <abs-path> [args]` (unquoted; the paths have no spaces) fixed it immediately: the same `git merge` was then denied live, and the marker proved the script ran on every Bash call. This also settled DEC-21's open question — **the exit-0 + `permissionDecision` JSON contract DOES work on this build** (the deny reason surfaced to the caller), so no revert to `exit 2` was needed. Root cause was purely the invocation form, not trust, not hot-reload, not the block contract. | Empirically isolated during the 2026-07-05 resume via a marker-file probe: broken form → no marker, tool proceeds; `pwsh -File` form → marker written, tool blocked. Supersedes the DEC-23 "needs a restart" hypothesis — settings-hook edits **did** hot-reload once the command was valid. |
| DEC-27 | **Subagent path guard verified LIVE (fresh session); subagent `gh pr merge` block verified by component-equivalence, not a single end-to-end probe run.** On the 2026-07-05 fresh-session resume the hooks loaded (an orchestrator `git merge --abort` was blocked, re-confirming activation). The `gatecheck-probe` agent (implementer's frontmatter guard, neutral prompt) ran a 4-call matrix: (1) Write to `backend/tests/` → **BLOCKED by `deny.ps1`** ("may not edit tests… locked contract") — spec-freeze does not cover `tests/`, so this block is unambiguously the per-role frontmatter guard; (2) Write to `design/` → **BLOCKED** (global spec-freeze won the race, also correct); (3) Write to `backend/` → **ALLOWED** (control: not a blanket-deny); (4) `gh pr merge 999999` → **reached `gh` unguarded** (GitHub's own "no such PR" error). #4 is expected, not a gate failure: the probe's block-merge Bash guard was **added mid-session**, and agent frontmatter does **not** hot-reload (re-confirms DEC-25), so the probe ran with its session-start frontmatter (path guard only). The subagent merge block is therefore covered by component-equivalence — every component is independently verified live: the `if: Bash(…)` conditional form is proven (tests-green fired only on `git commit`, DEC-24); the subagent frontmatter `pwsh -File` hook is proven (this probe's `deny.ps1`); `block-merge.ps1`'s deny is proven standalone **and** live via the global plugin-merge matcher — plus the real roles refuse to attempt a merge at all (DEC-25). A single end-to-end probe of the composition needs one more restart (the probe file now carries both guards on disk). | The one gate leg that cannot be exercised in a compliant session; its every constituent is verified live, so the residual risk is only the wiring composition, which mirrors a proven-live composition (tests-green). |
| DEC-29 | **TDD harness vendored @ `593e7abae2dc74f9a21eba3323e78a8fa9520dba` and adapted to Zij by override-block, not rewrite.** Copied the 6 skills from the upstream `.claude/skills/` tree into Zij's. **Audit (Checkpoint-4 deliverable): both write/delete scripts are safe** — `safe-cleanup/scripts/classify-branches.mjs` is dry-run by default, LOCAL-only (never touches remote), requires `--apply --yes` + a category flag, writes a mandatory recovery log before any delete and aborts if it can't, protects base/current/main, and re-verifies force-deletes at delete time; `safe-pr/scripts/collect-evidence.mjs` writes only under `docs/tdd-evidence/`, scans copied files for secrets (reports pattern+file, never the value), drops `.zip`/`.har` by default, and never clobbers `--out` without `--force`. **No skill merges** — `safe-pr` prepares the PR (push + `gh pr create`) and stops; none call `git merge`/`gh pr merge`/the plugin merge tool, so the harness composes cleanly under the Phase-3 gates. Both `.mjs` scripts run on **Node v24** (present); **kept as-is rather than reimplemented in Python** (80/20 — they are audited, cross-platform, and Node is a stated harness prerequisite; both smoke-tested live this session). **Adaptations:** each SKILL.md gets a top "Zij adaptation" override block (roles/gates/stack) plus surgical inline edits, preserving the well-crafted bodies. Key: **DEC-1 role split enforced in `red-green-refactor`** (implementer enters at the inner loop; the outer acceptance test is test-author-authored, committed red, and locked — the implementer may not create/edit it, and the path-guard hooks deny writes under `**/tests/` anyway); **non-web transcript evidence is the default** (Playwright only for a real `frontend/` slice); **`python-ci.yml` rewritten** to `uv sync` / `uv run pytest` / `uv run ruff check` on Python 3.13 (lint + unit jobs; validated parse); **`tdd-plan` template** gains an `Issue:` field (DEC-4) and DEC-1 role/ordering fields. `.gitignore` ignores `PR_BODY.md` + raw local test artifacts (keeps committed `docs/tdd-evidence/**`); `plans/` seeded. | Vendor-by-copy with a pinned SHA is reproducible; override-blocks adapt behavior without forking the upstream prose (easier to re-sync). The audit is the Checkpoint-4 gate; keeping Node scripts avoids a rewrite that buys nothing. |
| DEC-28 | **Subagent `gh pr merge` block verified LIVE end-to-end (fresh session, 2026-07-05).** The single composition gap DEC-27 left open is now closed. In a confirmed-fresh process (orchestrator `git merge --abort` blocked by git-guard on entry), the `gatecheck-probe` — now carrying both frontmatter guards on disk at session start — ran a 2-call matrix: (1) `gh pr merge 999999 --merge` → **BLOCKED**, deny text = `block-merge.ps1:6` verbatim ("…the founder merges via 'gh pr merge' from the main session"), which is *distinct* from both of git-guard's deny strings, and git-guard by design lets `gh pr merge` pass (DEC-22) — so the block is unambiguously the per-role frontmatter `block-merge.ps1`; (2) control `echo probe-control-ok` → **ran normally**, proving the guard is a targeted `if: Bash(gh pr merge *)` match, not a blanket Bash deny. Combined with the already-live path guard (DEC-27) and all global gates (DEC-24), **every leg of Phase 3 is now verified live.** | Closes the residual "wiring composition" risk from DEC-27 by exercising the actual composition (path guard + block-merge guard on one agent) in a fresh session, which is the only way agent frontmatter loads (DEC-25). |
| DEC-32 | **The GitHub plugin's personal-access token cannot create PRs (`403 Resource not accessible by personal access token`); PR creation uses the raw-`gh` fallback.** Opening PR #5 via `mcp__plugin_github_github__create_pull_request` returned 403 — the plugin's PAT lacks the write scope. This matches how prior phases opened PRs (`gh pr create`, implied by DEC-26). PR body passed via `gh pr create --body-file PR_BODY.md` (which is `.gitignore`d, DEC-29): this also keeps body text out of the Bash command string, sidestepping git-guard's DEC-26 substring scan entirely. **Consequence for `/sprint-plan`:** the plugin's `issue_write` may hit the same 403 (issue vs PR write are separate scopes, so untested — not verified live to avoid polluting the issue tracker), so the command now says **prefer the plugin, fall back to `gh issue create` on a 403.** Raw `gh` as a fallback is explicitly sanctioned by the Phase-5 reference. | The plugin token is read-biased on this repo; falling back to the founder-authenticated `gh` (which *can* write) keeps the workflow moving without widening the plugin's scope. |
| DEC-30 | **Phase-5 sprint layer is implemented as founder-invoked commands (`.claude/commands/*.md`), not model-triggered skills.** The reference calls them "skills" generically, but on this host both commands and skills surface as `/name`; the distinction is that a **command fires only when the founder explicitly types it**, whereas a skill can auto-trigger on a description match. Sprint orchestration must never auto-start mid-conversation — you do not want `/sprint-start` cutting a branch and dispatching subagents because a message happened to match a description. Commands give explicit-only invocation, and match the existing `build-resume` precedent. The harness building blocks (`tdd-plan`, `red-green-refactor`, …) stay skills because they are model-composed mid-task; the sprint layer that *drives* them is founder-initiated. | Explicit-only invocation is a real safety property for orchestration entry points; commands provide it, auto-triggered skills do not. |
| DEC-31 | **Five GitHub labels created via `gh label create` (raw-gh fallback, sanctioned by the reference for labels).** The installed GitHub plugin exposes `get_label` but **no label-create tool**, so label creation is the one place raw `gh` is used instead of the plugin (DEC-7's "raw gh only as a fallback"). Created: `spec-drift` (impl revealed spec wrong → deliberate spec-author pass), `blocked`, `needs-context` (= NEEDS_CONTEXT), `done-with-concerns` (= DONE_WITH_CONCERNS), and `sub:v0` seeding the **`sub:<name>` subproject-namespace convention** (one per lifecycle stage; `/sprint-plan` creates the next `sub:<name>` on demand). Label creation is additive/reversible and not an approval-gated action (not a merge/branch-protection/delete/push-to-`main`), so the orchestrator ran it directly. GitHub caps label descriptions at 100 chars — `spec-drift`'s first description was trimmed to fit. | Additive infra; the plugin can't create labels, and `gh label create` trips none of git-guard's tokens (no `git merge`/`push … main`/`git commit`/`gh api …merge`). |
| DEC-34 | **The implementer cannot author inner unit tests (the path guard blocks all `**/tests/` writes) — so in Zij, test authorship is fully owned by the test-author, and the implementer greens against the tests, it never writes them.** Vanilla `red-green-refactor` has the implementer write each inner failing unit test in the RED step; Zij's DEC-1 role split is stricter — `deny.ps1 -Role implementer` denies every write under `**/tests/` (deny.ps1:78-80), so the implementer physically cannot do the classic inner-RED. Resolution: (a) for a slice whose behavior is **fully pinned by the outer acceptance test** (e.g. this dry-run's `health_status()` — the single assertion `== {"status":"ok","service":"zij"}` checks type, key-set, and values at once), **no separate inner unit tests are needed**; the implementer drives production code straight against the locked outer test as its red/green signal. (b) For a richer slice that genuinely needs inner unit tests for internal collaborators, **the test-author authors those inner unit tests too** (it is the only role that may write under `**/tests/`), seeding them from the plan's unit list; the implementer greens them. The implementer's inner loop is therefore "write minimum production code → run the (test-author-authored) tests → refactor on green," never "write a failing unit test first." Surfaced by the Phase-6 dry run alongside DEC-33. **Consequence for this slice (compounds with DEC-33's handoff):** the implementer's only production change (`backend/health.py`) immediately makes the strict-xfail outer test XPASS → suite red → the implementer's own commit is blocked and it cannot clear the marker; so the implementer leaves `health.py` uncommitted in the working tree and hands back, and the **test-author** lands the final green commit (marker removed) which sweeps in that production delta. Attribution of the production code to the implementer lives in the plan's status log and the PR body, not in git author metadata for that one closing commit. | The roster (a locked DEC) already says the implementer writes "never tests"; the path guard enforces it. The only coherent reading is that all test authorship — outer and inner — belongs to the test-author, which strengthens the behavior-first contract (tests are never written by the party trying to pass them) at the cost of one extra test-author round-trip on test-heavy slices. |
| DEC-33 | **The "commit the outer test red" (DEC-1) vs "no commit on a red suite" (tests-green gate) contradiction is resolved with a strict-xfail marker (founder-approved at Checkpoint 6 entry, 2026-07-05).** DEC-1 / `test-author.md` / `sprint-start.md` all require the outer acceptance test **committed red before implementation**; the global `tests-green` hook (and the vendored philosophy doc line 273, and `red-green-refactor` SKILL step 14) forbid committing any red suite. Both are literally true only if the red outer test **exits pytest 0**. Resolution: the test-author commits the outer test decorated `@pytest.mark.xfail(reason="<behavior> not yet implemented", strict=True)`. While the behavior is absent the assertion fails → pytest reports `xfailed` → **exit 0** → the tests-green gate passes → **the red commit lands** (a real, dated, in-history artifact proving no implementation preceded it — DEC-1 satisfied — while the suite is green — the gate satisfied). **Lifecycle / role handoff:** the test-author **opens** the contract (xfail-strict red commit) and **closes** it (removes the marker on green); the implementer fills the middle and may never touch the marker (the `**/tests/` path guard blocks it). Because `strict=True`, the instant the implementer completes the behavior the test **XPASSes → suite red → the implementer's own final commit is blocked** and it cannot clear the marker — so the implementer greens the behavior, leaves the final state in the working tree (the sanctioned "one red test uncommitted" resume allowance), and hands back; the orchestrator then dispatches the **test-author** to remove the `xfail` marker and land the final fully-green commit (which finalizes the locked contract and sweeps in the implementer's last uncommitted production delta). Removing an `xfail` marker on a now-passing test **finalizes** the contract, it does not loosen it — and `strict=True` is deliberate: a non-strict marker would let a later regression silently drop back to `xfailed` (green), hiding it, whereas strict forces the marker gone so the test gates normally forever after. Chosen over (B) "red in working tree, commit on green" (rejected: weakens DEC-1's git-history proof of ordering to a mere transcript) and (C) excluding the acceptance test from the gate (rejected: erodes "no commit on a red suite" — a real acceptance regression could then be committed unblocked). | Only strict-xfail satisfies **both** constitutional rules simultaneously with zero change to the gate, keeps the ordering proof in git history, and stays regression-safe. Surfaced by the Phase-6 dry run — the first live attempt to commit a red outer test through the live gate — exactly what the dry run exists to catch (operating rule 7 + the Core Principle: raise the contradiction, don't produce a nonsensical deadlock). |
| DEC-35 | **Merge and safe-cleanup are approval-gated orchestrator actions, not founder-executed ones (founder-directed revision, 2026-07-05).** The founder re-framed the authority model: privileged actions *require founder approval and nothing more* — when a branch's work is complete, an explicit "approved" is all that's needed, and the **orchestrator** (main session) then runs the merge (`gh pr merge`) and the branch cleanup (`safe-cleanup --apply`) itself. Nothing is "reserved to the founder's hands." Mechanically this supersedes part of DEC-22: `git-guard.ps1` gained a `-Scope` parameter — **global** scope (settings.json, unchanged command line, so it took effect without a restart) now checks only *no-direct-commit-on-`main`*; the `git merge` / `gh api …merge` / push-to-`main` denies moved to **subagent** scope, wired as an unconditional frontmatter Bash hook (`git-guard.ps1 -Scope subagent`) on the four Bash-capable roles alongside the existing `gh pr merge` block-merge guard, and subagent scope additionally denies `git branch -d/-D` (cleanup is the orchestrator's). The global plugin-merge MCP block stays (subagents can reach that tool; the orchestrator merges via `gh`). Side effect: DEC-26's false positive class (orchestrator commits/PR bodies mentioning "git merge") disappears for the orchestrator, since global scope no longer scans merge tokens; the broad substring matching persists for subagents, where fail-safe bias still applies. Wording updated across `CLAUDE.md`, `safe-pr`/`safe-cleanup`/`tdd-harness` skills, and `/sprint-start` (which now ends with an explicit "merge and clean up on approval" step). A 10-case standalone allow/deny matrix verified both scopes; the frontmatter additions load on the next fresh session (agent frontmatter does not hot-reload, DEC-25/27). The upstream `agentic-engineering-org` skill was revised identically the same day. | The gate's purpose is that no unaccountable actor merges — founder *approval* preserves that entirely; founder *execution* was ceremony. Removing it also removes a live pain point (DEC-26 false positives) while keeping every subagent hard-blocked. |
| DEC-36 | **Operational docs aligned to DEC-34, and `safe-pr` body regen made deterministic (2026-07-05, after slice #10).** Slice #10 (`config`, PR #24) surfaced two harness frictions, neither a new policy — both were already-decided things the operational layer failed to reflect. (1) **Inner-test routing:** the orchestrator's own `/sprint-start` step 5 said only that the hooks "block it from editing the outer test," never that inner-test authorship belongs to the test-author — so the orchestrator mis-briefed the implementer to "write inner unit tests," which the implementer correctly refused (its agent file already encodes DEC-34) but which cost a round-trip. DEC-34 already resolved this; the fix is documentation alignment: `/sprint-start` step 5 now states the implementer writes **no** tests and spells out DEC-34's two cases (outer test fully pins the slice → no inner tests; else the **test-author** authors inner tests from the plan's unit list, folded into the marker-removal pass); `CLAUDE.md`'s behavior-first loop point 2 no longer says "may not edit the *outer* test" (too narrow — the guard blocks *all* `**/tests/`) but "may not edit any test," with test authorship, outer and inner, assigned to the test-author; `test-author.md` makes inner-test ownership explicit. No hook or gate changed — the `deny.ps1` guard was already correct. (2) **`safe-pr` PR body:** `collect-evidence.mjs --body-only` "never clobbers `--out` without `--force`" (DEC-29 safety property, `collect-evidence.mjs:348`), so a leftover `PR_BODY.md` from the prior slice made the run divert to `PR_BODY.generated.md` with only a `WARN` — easy to miss, leaving the stale prior body in place. Fix: the `safe-pr` SKILL step 5 command now passes `--force` (the PR body is git-ignored and regenerable, so clobbering is intended). | Both were "the decision was right, the runbook lagged it" gaps, not re-openings. Aligning the orchestration prose (which drives the orchestrator, the one actor with no path-guard to catch a mis-briefing) is the highest-leverage correction; the `--force` fix removes a silent-stale-body trap. Logged so the next slice's operator sees the corrected flow. |
| DEC-37 | **Skill-reconciliation pass (2026-07-08): the upstream `agentic-engineering-org` skill was revised after Zij adopted it (Jul 5), and the deltas were reconciled in on branch `setup/skill-reconcile` — preserving Zij's recorded adaptations (DEC-10 `design/`↔`backend/`↔`tests/`, DEC-21 JSON `permissionDecision` deny, DEC-33 strict-xfail over `allow-red-commit`, script names).** A fork diffed the skill's current `references/` skeletons against every live artifact and classified each divergence APPLY/KEEP/UNSURE; the founder approved the three judgment calls. Changes: **(1) Double-wiring the critical gates (DEC-18 finally realized).** `deny.ps1` (path-guard) and `git-guard.ps1` now run **both** in role frontmatter *and* globally in `settings.json`; the global layer keys on stdin `agent_type` — present only in subagent calls — so a stale frontmatter snapshot (GH #18392) can no longer silently disable a gate, while the orchestrator (no `agent_type`) still passes. `deny.ps1 -Role` is now optional (falls back to `agent_type`; fail-closed only when a role is pinned by frontmatter; non-writing subagents like Explore pass). `git-guard.ps1` auto-detects subagents and its subagent set absorbed `gh pr merge`, so the separate `block-merge` frontmatter guard (and its `if:` filter) was removed from all four Bash roles. **(2) Dropped every `if:` permission-rule filter (DEC-16):** the `if: Bash(git commit *)` on tests-green and `if: Bash(gh pr merge *)` in frontmatter were dodgeable by compound commands (`x && git commit`); scripts now self-detect from stdin. `tests-green.ps1` self-detects `git commit`, adds a docs-only fast path and worktree-aware `cwd` root resolution, keeps the **full** suite per commit and strict-xfail (no `allow-red-commit`). **(3) `block-merge.ps1` is now tool-aware** and the MCP matcher extended to `create_or_update_file\|push_files\|delete_file`: plugin direct-writes targeting `main` are blocked for everyone (DEC-7/10). **(4) Spec-freeze toggled by the gitignored `.claude/spec-mode` flag file** instead of commenting the hook out (no settings edit to forget to restore). **(5) Additive:** `format.ps1` PostToolUse formatter (ruff format, never blocks); reviewer gains confidence-scoring (report only findings ≥ 80); `/sprint-start` gains the two mandatory dual-register founder briefs (kickoff before the branch is cut, wrap-up at the pause). A 27-case script-level exit battery + live checks of the flag lift and docs-only skip all pass (orchestrator merge/push stay open; subagents blocked incl. compound commands; plugin direct-write-to-main blocked). **Live in-session verification of the new wiring is deferred to a fresh session (DEC-23): hooks/frontmatter do not hot-reload.** | The skill's `agent_type` double-wiring is strictly stronger than DEC-35's frontmatter-only subagent scope — it closes the stale-snapshot gap AND keeps the orchestrator open — so it supersedes that narrow choice; dropping `if:` filters closes a real compound-command dodge of both the commit and merge gates. Everything else is additive or a lower-risk mechanism swap. Ported into Zij's conventions rather than adopting the skill's filenames verbatim (practicality over perfectionism). |
| DEC-38 | **Iteration-loop economics (founder-adjudicated 2026-07-10, after slice #62 burned ~90 min / ~275k tokens of implementer iteration): the org priced *who may do what* but not *how fast each actor learns whether it's right* — five operational rules now govern that, and the upstream `agentic-engineering-org` skill was revised identically the same day (DEC-37 precedent).** **(1) The red-commit marker is a landing device, not an iteration signal.** DEC-33's strict-xfail / `test.fail()` exists solely to land the red outer commit under the tests-green gate; while marked, the test is diagnostically blind BOTH ways — a failing body reports "passed" with all error detail suppressed, a passing body reports failure (and with `screenshot: only-on-failure` against a held-open SSE+MapLibre page, *hangs*, so success presents as a hang — this exact signature cost most of #62's burn). Nobody iterates against a marked test; the moment implementation is believed complete, the marker comes off for the verification run (orchestrator may strip it directly on a verified pass — #53/#62 precedent; reviewer validates non-loosening). **(2) Unit-first: the fast signal must exist before the implementer starts.** DEC-34's routing (all tests → test-author) had inner units landing in the *closing* marker-removal pass, leaving the implementer's only oracle the slowest loop in the system (e2e with real wall-clock waits + the environmental teardown tail). The plan's inner unit list is authored before/alongside implementation; the outer test runs ONCE, time-bounded, at milestones — extending DEC-37's tiered-suite principle one level down (no e2e inside the implementer's loop, just as no full suite inside the commit loop). **(3) A locked test's unreached tail is unverified at authoring time** — a red-by-construction test executes only to its first failing line; #62's outer test hid two green-time defects past that line (`getPaintProperty('icon-rotate')` throws unconditionally — layout property; a uniform-age 4-vessel fixture made "only V2 drops" unsatisfiable by ANY correct implementation). Authoring a locked test against absent behavior demands explicit scrutiny of the unreached tail (check API usage against merged sibling tests; hand-verify fixture math), and green-time test defects route like spec-drift: implementer proves (cannot fix), test-author fixes, reviewer verifies non-loosening, orchestrator adjudicates — exercised on #62, reviewer PASS/PASS confirmed zero loosening. **(4) The orchestrator is the escalation valve for non-converging loops:** subagent ceremony is for building features, not debugging harness pathologies; on a burn, the orchestrator verifies the subagent's claims independently and applies diagnosed mechanical fixes directly (reviewer still gates, founder still approves merges). Standing rule for Zij frontend: **"Direct + unit-first."** **(5) Environmental facts are measured once, recorded, routed around:** the ~5-min chromium worker-teardown hang was proven environmental by an `about:blank` probe (page-close ordering and launch-args changes made zero difference) — every e2e run is bounded to the *real* work (~60–140s), the result line is the verdict, the force-kill is noise. Corollary hygiene: a piped `$?` reports the last pipe stage, not your command; a wall time landing exactly on your own timeout means the timeout fired. | One slice's outer-loop burn cost more than the entire two-bug follow-up (#98+#101: diagnosis→merged in ~15 min under the new rules, 182 Vitest green in ~5 s per iteration). The gates were never the problem — they held throughout, including against the orchestrator itself. The missing constitution was feedback latency; these rules price it. |
| DEC-23 | **Hooks do not hot-reload into an already-running session on this host.** Empirically confirmed: after writing `settings.json` + scripts, a temporary always-fire diagnostic hook never executed, `git-guard` did not block an orchestrator `git merge`/push-to-`main`, and the implementer's frontmatter `deny.ps1` did not block a write under `backend/tests/`. All five scripts pass a standalone allow/deny matrix. **Live gate verification is therefore deferred to a fresh session** (founder restarts Claude Code), after which the orchestrator re-runs the full live suite before Checkpoint 3. | Docs claim a file-watcher picks up hook edits, but it did not fire here (session predates the hooks / Windows watcher). The scripts are proven; only in-session *activation* is pending a restart. **Reconfirmed on the 2026-07-05 resume: running `/build-resume` inside the existing session does NOT load the hooks — an orchestrator `git merge --abort` executed unblocked. The founder must fully quit and relaunch Claude Code (a new process), then review/trust the hooks via `/hooks`, so the `PreToolUse` gates in `.claude/settings.json` are actually registered. Only then can the orchestrator run the live Checkpoint-3 suite.** |
| DEC-39 | **A sixth role, the `fixer`, plus a `/fix` command, add a fast lane for bugs and small changes that don't warrant a full slice (founder-adjudicated 2026-07-11).** The org priced *who may do what* and the behavior-first slice loop, but forced *every* change — a typo, a one-line guard, a config bump — through the full triage→spec→test→implement→review ceremony or through the orchestrator's own hands (DEC-38(4)). The fixer right-sizes that. **New artifacts:** `.claude/agents/fixer.md` (model `sonnet`; the implementer's exact tool set and write-scope — product code under `backend/`/`frontend/`, never `design/` or `**/tests/`); `.claude/commands/fix.md` (a **command**, not an auto-triggering skill — DEC-30: a branch-cutting, subagent-dispatching entry point must be explicit-only). **`/fix` classifies into three buckets:** *non-behavioral* (fixer changes product code, the existing suite stays green, commit on the `fix/` branch), *behavioral bug* (a stripped test-first loop — the **test-author** commits **one** regression test red via strict-xfail per DEC-33, the fixer greens it, the test-author clears the marker per DEC-34; no spec, no outer-acceptance ceremony, no two-stage review), and *feature-scale*, **bounced to `/sprint-start`** — that bucket, plus the fixer's own BLOCKED-on-scope-creep report, is the guard that keeps the fast lane from becoming the default. **The only mechanism change is one switch arm.** `deny.ps1` gained a `'fixer'` case identical to `'implementer'` (deny `design/`, deny `**/tests/`). It is *required* despite the identical scope: the global backstop layer keys on stdin `agent_type` (DEC-37), and a `fixer` agent_type not present in the switch would hit `default`, which on the non-explicit global layer *passes through* — so without the case the fixer's path guard would be frontmatter-only, re-opening exactly the stale-snapshot gap DEC-37 closed. **Everything else binds the fixer for free** because the hard gates key on *agent_type presence*, not role name: `git-guard.ps1 -Scope subagent` (+ its global `agent_type` leg) blocks the fixer's merges/pushes-to-`main`/branch-deletes and no-commit-on-`main`; `tests-green.ps1` self-detects `git commit` and blocks a red suite; the `block-merge` MCP matcher covers the plugin. **The boundary is unchanged:** the change still lands as a PR into `main`, founder approval still gates the merge, and the orchestrator still runs the merge + `safe-cleanup`. Ceremony is relaxed; the two hard gates and the approval seat are not. **Relation to DEC-38(4):** orchestrator-direct fixes (during a non-converging burn) stay valid; the fixer is the *dispatchable, path-guarded* complement for standalone small work — orchestrator-direct runs under no path guard (the orchestrator carries no `agent_type`) and consumes the orchestrator's context, whereas the fixer keeps even a quick fix under the guard and off the main thread. `CLAUDE.md` gains a "fix lane" section, a sixth roles-table row, and "six subagents"; `path-guard.test.ps1` (new) covers the fixer arm alongside the three existing roles. The upstream `agentic-engineering-org` skill was revised identically the same day (DEC-37/DEC-38 precedent), in its own generic conventions (`path-guard.ps1`, `block-merge.ps1`, `specs/`/`src/`/`tests/`, exit-2). **Live activation deferred to a fresh session (DEC-23): agent frontmatter and hooks do not hot-reload.** | A bug is not a feature; forcing it through feature ceremony violates the 80/20 Developer Principle for no safety gain. The safety surface grew by exactly one line because the gates were designed to key on `agent_type`, not on an enumerated role list — so a new non-merging, path-guarded actor inherits every gate the moment it has an `agent_type`, and only the one allowlist that enumerates roles (`deny.ps1`) needed teaching. Keeping `/fix` a command (not a skill) preserves DEC-30's explicit-only property for orchestration entry points. |

---

## GitHub plugin — recorded tool names (Phase 0 deliverable)

Enumerated from the live tool registry (the `/plugin` slash command belongs to
the founder's main session; **please confirm at Checkpoint 0**). Namespace:
`mcp__plugin_github_github__`.

**Merge-capable / write tools the *agents-never-merge* gate (Phase 3) must cover:**
- `mcp__plugin_github_github__merge_pull_request` — **the merge tool; primary gate target.**
- `mcp__plugin_github_github__pull_request_review_write` — can submit reviews (approve).
- `mcp__plugin_github_github__update_pull_request_branch`
- `mcp__plugin_github_github__create_or_update_file` / `push_files` / `delete_file` — direct writes to a ref.

**Issue/PR tools for the sprint workflow (Phase 5):**
- `issue_write`, `sub_issue_write`, `add_issue_comment`, `issue_read`, `list_issues`, `search_issues`, `list_issue_types`
- `create_pull_request`, `pull_request_read`, `list_pull_requests`, `add_comment_to_pending_review`

The plugin also bundles a `pr-review-toolkit` (inspect in Phase 2 before writing
the reviewer subagent).
