# Plans

Per-slice TDD execution plans live here, written by `/tdd-plan` as `plans/<feature-slug>/`.
Each slice plan links a GitHub issue (the system of record, DEC-4) and is the locked
contract the test-author and implementer build against (DEC-1). Empty until the first sprint.
